# Activity and evaluation matrix redesign

This change keeps formative fields on activities, not projects. Activities now have a date and a compact identifier. Evaluation uses a student-by-activity matrix while preserving delivery status and achievement levels. Desktop scrolling accepts mouse, trackpad, touch, and stylus dragging.

The Windows installer pipeline remains validated, but a public production installer still requires Windows code signing and an explicit release/tag.
